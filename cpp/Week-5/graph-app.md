# Real Life Application of a Graph

Solution :
Flight Networks

Each airport is a node.
Each flight route is an edge.
Weights can represent ticket price, flight duration and distance.

A graph is suitable because airports connect to many others and since multiple routes exist between 2 cities, Airlines can find the cheapest or fastest route.