# Real Life Application of a Tree

Solution :
Folder Structure on a Computer.

Since folders contain subfolders, and those subfolders contain more subfolders, we require a tree which represents hierarchy naturally.

It also becomes easy to navigate because the operating system simply follows the path from the root to the required folder.

It enables easy organization because adding or deleting folders becomes simple. We can create or remove an entire branch without affecting unrelated folders.